//! Wasm entry point for the Cloudflare Worker.
//!
//! Binds R2 + D1 + KV and routes into core use-cases.
//! Wired with `workers-rs` in a later task (doc02.02.00).

#![allow(dead_code)]

/// Placeholder until `workers-rs` fetch handler is wired.
pub fn entrypoint_placeholder() {}
