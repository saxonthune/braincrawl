//! Shared SQLite-dialect query strings used by `store-d1` and `store-sqlite` (doc02.04).
//!
//! No real queries yet — this crate is a home for future shared SQL.

#![allow(dead_code)]

/// Placeholder pointer to the migrations directory.
pub const MIGRATIONS: &str = "migrations";
